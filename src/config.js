// Project-wide settings.
//
// QUIZ_CHOICES: how many multiple-choice options to show per quiz question.
// Decks for this project author each card with a CORRECT answer (`back`) plus a
// POOL of 5+ incorrect answers (`distractors`). Quiz mode shows the correct
// answer plus (QUIZ_CHOICES - 1) distractors chosen at random from that pool, so
// repeated attempts surface different wrong options. See README "Deck JSON format".
export const QUIZ_CHOICES = 4
